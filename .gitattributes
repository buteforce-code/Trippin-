# Normalize line endings to LF in the repo (keeps Linux CI + diffs clean).
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.woff binary
*.woff2 binary
